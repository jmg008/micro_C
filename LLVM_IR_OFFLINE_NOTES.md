# micro_C LLVM IR 오프라인 진행 노트

이 문서는 오프라인 상태에서 이어서 공부할 수 있도록 현재 저장소 상태와 LLVM IR 제작까지의 순서를 정리한 것이다. 저장소가 학습 모드이므로, 아래 내용은 완성 코드를 대신하지 않고 어디를 보고 무엇을 생각해야 하는지 안내하는 로드맵으로 작성했다.

## 1. 현재 저장소 상태 요약

현재 프로젝트는 작은 C 계열 언어를 직접 구현해 보는 단계로 보인다.

구성 파일:

- `Token.h`, `Token.cpp`: 토큰 종류와 문자열 변환.
- `Lexer.h`, `Lexer.cpp`: 소스 문자열을 토큰 배열로 바꾸는 스캐너.
- `Node.h`: AST 노드 구조 초안.
- `Parser.h`, `Parser.cpp`: 토큰 배열을 AST로 바꾸는 파서 초안.
- `README.md`: 아직 제목만 있음.
- `AGENTS.md`: 이 저장소는 학습 모드이며, 직접 구현 대신 설명과 힌트 중심으로 진행해야 함.

큰 흐름은 다음과 같다.

```text
source text
  -> Lexer
  -> vector<Token>
  -> Parser
  -> AST
  -> semantic analysis
  -> LLVM IR
```

현재는 `Lexer`와 토큰 정의가 어느 정도 있고, `Parser`와 `Node`는 설계 중간 단계다. LLVM IR 제작으로 바로 넘어가기보다는 AST와 의미분석의 최소 골격을 먼저 세워야 한다.

## 2. 토큰과 Lexer 상태

관련 파일:

- `Token.h`
- `Token.cpp`
- `Lexer.h`
- `Lexer.cpp`

현재 토큰 범위:

- 리터럴: `null`, 정수, 실수, 문자열, 문자, 식별자.
- 타입 키워드: `Int`, `Char`, `Void`, `Pointer`, `Array`, `Struct`.
- 선언/문장 키워드: `function`, `return`, `for`, `break`, `continue`, `if`, `else`, `print`, `printLine`.
- 연산자: 논리, 대입, 산술, 비교, 주소/포인터 관련 기호.
- 구분자: 괄호, 중괄호, 대괄호, 쉼표, 콜론, 세미콜론.

Lexer가 하는 일:

- 공백을 건너뜀.
- 숫자를 정수 또는 실수 리터럴로 읽음.
- 문자열/문자 리터럴에서 escape 문자를 처리하려고 함.
- 식별자가 키워드인지 확인함.
- 연산자/구두점을 가능한 가장 긴 토큰으로 줄여 가며 인식함.
- 마지막에 `EndOfToken`을 추가함.

오프라인 점검 포인트:

- `Lexer`에 `sourceCode`를 넣는 통로가 아직 보이지 않는다. 생성자나 setter 같은 입력 방식이 필요할 수 있다.
- 주석 처리 규칙이 아직 없다.
- 숫자 리터럴 규칙이 단순하다. 예를 들어 `1.`, `.5`, `1e3` 같은 형태를 지원할지 결정해야 한다.
- 문자열/문자 escape 오류를 어디까지 허용할지 정해야 한다.
- 토큰에 행/열 위치가 없어서 오류 메시지를 자세히 만들기 어렵다. 처음에는 생략해도 되지만, 디버깅이 힘들어지면 위치 정보를 고려한다.

작은 확인 질문:

- 지금 언어에서 키워드는 `Int`처럼 대문자로 시작하는 형태가 맞는가?
- `FloatLiteral` 토큰은 있지만 타입 키워드 `Float`는 없다. 실수 연산을 지원할지, 리터럴만 임시로 둔 것인지 정해야 한다.

## 3. AST 상태

관련 파일:

- `Node.h`

현재 잡혀 있는 AST 범주:

- 최상위: `Program`, `Declaration`
- 선언: `Function`, `GrobalVariable`
- 타입: `Type`, `Pointer`, `Array`
- 문장: `Return`, `For`, `Break`, `Continue`, `If`, `Print`, `ExpressionStatement`
- 표현식: 논리, 비교, 산술, 단항, 대입, 호출, 주소, 역참조, 인덱스, 변수, 멤버 접근, 리터럴

좋은 점:

- 선언, 문장, 표현식을 나누려는 방향은 LLVM IR로 넘어갈 때도 유용하다.
- 포인터, 배열, 함수, 제어문 같은 LLVM IR 학습에 좋은 재료가 이미 목표에 들어 있다.

오프라인 점검 포인트:

- `GrobalVariable`에서 `Expression*`을 사용하지만 `Expression` 선언이 그 아래에 있다. C++에서는 이 위치 관계를 조정하거나 전방 선언이 필요할 수 있다.
- `GrobalVariable`, `Assigment`처럼 철자가 흔들리는 이름이 있다. 동작에는 직접 영향이 없을 수 있지만, 나중에 검색과 유지보수에서 헷갈릴 수 있다.
- 모든 노드의 `generate()`가 `void`를 반환한다. LLVM IR 생성 단계에서는 표현식이 보통 "값"을 만들어야 하므로, 표현식 생성과 문장 생성을 같은 반환형으로 둘지 고민해야 한다.
- 노드 소유권이 전부 raw pointer다. 학습 초기에는 가능하지만, 어느 시점부터는 메모리 관리 전략을 정해야 한다.
- 각 표현식의 타입 정보를 어디에 저장할지 아직 보이지 않는다. LLVM IR 전에 타입 검사 결과를 담을 자리가 필요하다.
- lvalue/rvalue 구분이 아직 없다. 대입, 주소 연산, 역참조, 배열 인덱싱을 구현할 때 중요한 개념이다.

핵심 개념:

- rvalue: 계산된 값. 예: `x + 1`의 결과.
- lvalue: 값을 저장할 수 있는 위치. 예: 변수 `x`, 역참조 `*p`, 배열 원소 `a[i]`.
- LLVM IR에서 변수에 대입하려면 보통 먼저 "주소"가 필요하고, 값을 읽으려면 그 주소에서 `load`가 필요하다.

## 4. Parser 상태

관련 파일:

- `Parser.h`
- `Parser.cpp`

현재 구현된 방향:

- `parse()`는 최상위에서 `Int`, `Char`, `Void`로 시작하는 선언을 읽으려 한다.
- `parseDeclaration()`은 함수 선언/정의와 전역변수를 구분하려 한다.
- `parseType()`와 `parseType_reg()`는 C 스타일 선언자 일부를 처리하려 한다.
- `parseParameter()`는 함수 매개변수 목록을 읽는다.
- `parseBlock()` 이후 문장과 표현식 파싱은 아직 비어 있거나 선언만 있다.

중요한 현재 위험 지점:

- `Parser`의 `tokens`를 외부에서 넣는 통로가 아직 보이지 않는다.
- `parse()`에서 예상하지 못한 토큰을 만나면 `current`가 전진하지 않아 무한 루프가 날 수 있다.
- `parseBlock()`은 아직 문장을 읽지 않고 반환도 없다.
- 전역변수 초기화의 `=` 처리 부분이 `구현예정` 상태다.
- `parseExpressionStatement`, `parseVariable`, `parseFor`, `parseIf`, `parseReturn`, `parseContinue`, `parseBreak`는 선언되어 있지만 구현이 보이지 않는다.

파서를 이어서 만들 때의 최소 순서:

1. 토큰 입력 방식 정하기.
2. `parse()`가 항상 성공하거나 명확히 실패하게 만들기.
3. 블록 안에서 문장을 하나씩 읽는 구조 만들기.
4. 문장 종류를 구분하기: `return`, `if`, `for`, `break`, `continue`, `print`, 지역변수 선언, 표현식 문장.
5. 표현식 우선순위를 낮은 것부터 높은 것까지 나누기.
6. 파싱 실패 시 어떤 토큰이 필요했는지 알려주기.

표현식 우선순위 예시:

```text
assignment
logical_or
logical_and
equality
relational
additive
multiplicative
unary
postfix
primary
```

이 목록은 전체 구현이 아니라 설계 순서를 잡기 위한 힌트다. 각 단계가 "자기보다 강한 결합력의 단계"를 호출한다고 생각하면 된다.

## 5. LLVM IR 전에 필요한 의미분석

파싱이 되었다고 바로 LLVM IR을 만들 수 있는 것은 아니다. LLVM IR은 타입과 제어 흐름이 분명해야 하므로, 최소한의 의미분석이 필요하다.

필요한 정보:

- 스코프: 전역, 함수, 블록 내부.
- 심볼 테이블: 이름 -> 선언 정보.
- 함수 시그니처: 반환 타입, 매개변수 타입, 이름.
- 변수 타입: 전역변수, 매개변수, 지역변수.
- 표현식 타입: 산술 결과, 비교 결과, 포인터/배열 원소 타입.
- lvalue 여부: 대입 왼쪽에 올 수 있는지.
- 제어문 상태: `break`와 `continue`가 반복문 안에 있는지.
- 반환 검사: 함수 반환 타입과 `return` 표현식 타입이 맞는지.

타입 검사에서 확인할 질문:

- `Int`는 LLVM에서 `i32`로 둘 것인가, `i64`로 둘 것인가?
- `Char`는 `i8`로 둘 것인가?
- 비교 연산 결과는 내부적으로 `i1`인가, 언어 차원에서 `Int`인가?
- `null`은 모든 포인터 타입에 들어갈 수 있는가?
- 배열 이름은 값인가, 첫 원소 포인터처럼 취급할 것인가?
- 함수 호출 결과 타입은 어디에 저장할 것인가?
- `print`와 `printLine`은 언어 내장 문장인가, 런타임 함수 호출로 낮출 것인가?

처음에는 너무 많은 C 규칙을 따라가려 하지 말고, 작은 규칙을 명확히 정하는 편이 좋다.

## 6. LLVM IR 생성 방식 선택

두 가지 길이 있다.

### A. 텍스트 LLVM IR 직접 출력

장점:

- LLVM 라이브러리 연결 없이 시작할 수 있다.
- IR 문법을 직접 보며 배우기 좋다.
- 작은 언어에서는 빠르게 결과를 확인할 수 있다.

단점:

- 타입과 이름, 임시값 번호, 블록 라벨을 직접 관리해야 한다.
- IR 문법 실수가 나기 쉽다.
- 규모가 커지면 유지보수가 어렵다.

### B. LLVM C++ API 사용

장점:

- `Module`, `IRBuilder`, `LLVMContext` 같은 구조가 IR 생성을 도와준다.
- 타입 객체와 값 객체를 직접 연결할 수 있다.
- 검증기 사용이 쉽다.

단점:

- 빌드 설정이 먼저 필요하다.
- API 학습량이 있다.
- 현재 프로젝트 구조가 아직 빌드 시스템을 갖추지 않았으므로 준비 작업이 늘어난다.

학습 추천:

- 1단계: 아주 작은 부분집합에 대해 텍스트 IR을 손으로 작성해 본다.
- 2단계: AST에서 텍스트 IR을 출력해 본다.
- 3단계: 구조가 잡히면 LLVM C++ API로 옮길지 결정한다.

## 7. micro_C 타입을 LLVM 타입으로 매핑하기

초기 매핑 후보:

```text
Void       -> void
Int        -> i32 또는 i64
Char       -> i8
Pointer T  -> ptr
Array N T  -> [N x T]
Function   -> LLVM function type
String     -> 전역 상수 배열 + 포인터
Boolean    -> i1
```

주의:

- 최신 LLVM은 opaque pointer 때문에 `ptr`을 많이 쓴다.
- 배열 값과 배열 원소 주소는 다르다.
- 문자열 리터럴은 보통 전역 상수로 만들고, 표현식에서는 그 시작 주소를 사용한다.
- 구조체를 지원하려면 멤버 이름, 멤버 순서, 멤버 타입을 저장하는 별도 타입 정보가 필요하다.

## 8. LLVM IR 생성에 필요한 기본 개념

Module:

- 하나의 컴파일 단위.
- 전역변수, 함수, 문자열 상수 등이 들어간다.

Function:

- 반환 타입, 이름, 매개변수 목록을 가진다.
- 여러 BasicBlock으로 구성된다.

BasicBlock:

- 명령어가 순서대로 들어가는 블록.
- 마지막은 보통 `ret`, `br` 같은 terminator여야 한다.

SSA value:

- LLVM IR의 계산 결과는 보통 한 번 정의되고 재사용된다.
- 지역 변수처럼 값이 바뀌는 것들은 보통 `alloca`, `load`, `store`로 표현한다.

Control flow:

- `if`는 조건 블록, then 블록, else 블록, merge 블록을 생각하면 된다.
- `for`는 초기화, 조건, 본문, 증감, 종료 블록을 생각하면 된다.
- `break`와 `continue`를 위해 현재 반복문의 종료/증감 블록을 기억해야 한다.

Pointer and memory:

- 변수의 주소가 필요하면 `alloca`나 전역변수 주소를 사용한다.
- 값을 읽을 때는 `load`.
- 값을 쓸 때는 `store`.
- 배열 원소나 구조체 멤버 주소는 `getelementptr` 계열 개념이 필요하다.

## 9. 추천 마일스톤

### M0. 현재 코드가 어떤 단계인지 확인

목표:

- 모든 파일을 읽고, 어느 함수가 미완성인지 표시한다.
- 빌드가 된다면 어떤 컴파일 오류가 먼저 나는지 확인한다.

힌트:

- 지금은 `Node.h`의 선언 순서, `parseBlock()` 반환, 미구현 함수들이 먼저 걸릴 가능성이 있다.

### M1. Lexer 단독 확인

목표:

- 간단한 입력을 토큰 목록으로 바꾸는지 확인한다.

직접 만들어 볼 입력 후보:

```text
Int main() { return 1; }
Char c;
Int *p;
Int a[3];
```

확인할 것:

- 키워드가 식별자로 잘못 나오는지.
- `*`, `[`, `]`, `;`가 의도대로 나오는지.
- 마지막에 `EndOfToken`이 붙는지.

### M2. 선언 파싱 확인

목표:

- 전역변수 선언과 함수 선언/정의를 AST로 구분한다.

확인할 것:

- `Int x;`
- `Int main();`
- `Int main() { ... }`
- 포인터와 배열 선언.

생각할 질문:

- `Int *a[3]`와 `Int (*a)[3]`는 AST가 어떻게 달라져야 하는가?

### M3. 블록과 문장 파싱

목표:

- `{ ... }` 안에서 문장을 하나씩 읽고, 각 문장 노드로 만든다.

처음 지원할 문장 후보:

- `return`
- 표현식 문장
- 지역변수 선언

그 다음:

- `if`
- `for`
- `break`
- `continue`
- `print`

중요한 원칙:

- 반복문 안에서는 반드시 현재 토큰이 전진하거나 오류가 나야 한다.
- 빈 블록도 정상 처리되어야 한다.

### M4. 표현식 파싱

목표:

- 연산자 우선순위대로 AST가 만들어지게 한다.

시작 순서:

- primary: 리터럴, 변수, 괄호
- postfix: 호출, 배열 인덱스, 멤버 접근
- unary: `!`, `-`, `&`, `*`
- multiplicative/additive
- relational/equality
- logical and/or
- assignment

확인할 입력 후보:

```text
a + b * c
(a + b) * c
*p + 1
a[i]
f(x, y)
x = y + 1
```

### M5. 의미분석

목표:

- AST에 타입과 이름 정보를 연결한다.

필요한 구조:

- 전역 심볼 테이블.
- 함수별 지역 심볼 테이블.
- 블록별 스코프 스택.
- 함수 반환 타입 추적.
- 반복문 블록 추적.

확인할 오류 후보:

- 선언되지 않은 변수 사용.
- 함수 인자 개수 불일치.
- 포인터가 아닌 값에 역참조.
- 대입 불가능한 표현식의 왼쪽에 대입.
- 반복문 밖의 `break`.
- 반환 타입 불일치.

### M6. 가장 작은 LLVM IR 생성

목표:

- 함수 하나와 `return` 하나를 IR로 표현한다.

처음에는 아래 개념만 필요하다.

- module
- function
- entry block
- return
- 정수 상수

여기서 바로 `if`, `for`, 포인터까지 가지 말고, `return 1;` 같은 아주 작은 AST가 IR 조각으로 내려가는 경험을 먼저 만든다.

### M7. 지역변수와 대입

목표:

- 지역변수 저장 공간, 값 읽기, 값 쓰기를 구분한다.

필요한 개념:

- `alloca`
- `store`
- `load`
- expression codegen이 값 또는 주소 중 무엇을 돌려주는지.

생각할 질문:

- 변수 이름을 만나면 바로 값을 읽을 것인가?
- 대입의 왼쪽에서는 왜 값이 아니라 주소가 필요한가?

### M8. 산술/비교/논리 연산

목표:

- `+`, `-`, `*`, `/`, `%`, 비교, 논리 연산을 IR로 낮춘다.

주의:

- 비교 결과는 보통 `i1`이다.
- 언어에서 `if (x)`를 허용한다면 `x`를 조건으로 바꾸는 규칙이 필요하다.
- `&&`, `||`는 단순 산술 연산이 아니라 short-circuit 제어 흐름으로 처리할지 결정해야 한다.

### M9. if와 for

목표:

- 조건 분기와 반복문을 BasicBlock 단위로 표현한다.

생각할 블록:

- `if`: condition, then, else, merge.
- `for`: init, condition, body, step, exit.

주의:

- 각 BasicBlock 끝에 terminator가 있는지 확인한다.
- 이미 `return`으로 끝난 블록에 또 branch를 붙이지 않도록 조심한다.

### M10. 포인터, 배열, 문자열

목표:

- 주소 계산과 메모리 접근을 안정적으로 처리한다.

필요한 개념:

- 주소 연산 `&`
- 역참조 `*`
- 배열 인덱싱
- 문자열 리터럴 전역화
- null pointer

주의:

- 배열 자체의 타입과 배열 원소 주소의 타입을 구분한다.
- 문자열 리터럴은 지역 임시값처럼 만들기보다 module-level constant로 두는 편이 자연스럽다.

### M11. print/printLine

목표:

- 언어 내장 출력 문장을 런타임 호출 또는 외부 함수 호출로 낮춘다.

생각할 질문:

- `print`는 어떤 타입들을 받을 수 있는가?
- 정수, 문자, 문자열마다 다른 출력 함수를 둘 것인가?
- `printLine`은 마지막에 newline을 따로 출력할 것인가?

## 10. IR 생성기 설계 시 필요한 상태

IR 생성기는 보통 다음 상태를 들고 있다.

- 현재 module.
- 현재 function.
- 현재 basic block 또는 insertion point.
- 전역 심볼 테이블.
- 현재 함수의 지역 변수 주소 테이블.
- break 대상 블록 스택.
- continue 대상 블록 스택.
- 문자열 리터럴 번호 생성기.
- 임시값 이름 생성기.

표현식 생성에서 자주 나뉘는 두 질문:

- 이 표현식의 값을 원하나?
- 이 표현식이 가리키는 저장 위치 주소를 원하나?

예를 들어:

- `x`를 읽을 때는 `x`의 주소에서 load한 값이 필요하다.
- `x = 3`의 왼쪽 `x`는 load하면 안 되고 주소가 필요하다.
- `&x`는 `x`의 주소가 결과값이다.
- `*p`는 문맥에 따라 주소로도, 값으로도 쓰일 수 있다.

## 11. 지금 코드에서 먼저 살펴볼 위치

우선순위가 높은 위치:

- `Parser.h`: `tokens` 입력 방식이 필요하다.
- `Parser.cpp`의 `parse()`: 알 수 없는 토큰에서 전진하지 않는 흐름을 확인한다.
- `Parser.cpp`의 `parseBlock()`: 블록 내부 문장 파싱의 중심이 될 자리다.
- `Node.h`: `Expression` 선언 위치와 각 노드의 `generate()` 반환 설계를 검토한다.
- `Lexer.h`: `sourceCode` 입력 방식을 검토한다.

직접 점검할 체크리스트:

- 파서가 토큰 배열을 받을 방법이 있는가?
- 모든 반복문은 토큰을 소비하거나 오류를 내는가?
- `parseBlock()`은 빈 블록과 여러 문장 블록을 모두 처리할 수 있는가?
- 표현식 파싱 함수들은 우선순위를 명확히 나누는가?
- AST 노드가 의미분석 결과 타입을 담을 수 있는가?
- IR 생성에서 표현식 값과 주소를 구분할 수 있는가?

## 12. 오프라인으로 이어갈 때의 권장 순서

1. 현재 코드가 컴파일되는지 확인한다.
2. 가장 먼저 나오는 컴파일 오류를 하나만 고른다.
3. 그 오류가 선언 순서 문제인지, 미구현 함수 문제인지, 반환 누락 문제인지 분류한다.
4. Lexer 입력 방식과 Parser 입력 방식을 정한다.
5. `Int main() { return 1; }` 하나를 목표 프로그램으로 정한다.
6. 그 프로그램을 토큰으로 만든다.
7. 그 토큰을 AST로 만든다.
8. AST를 손으로 그려 본다.
9. 타입 검사가 필요한 지점을 표시한다.
10. 아주 작은 LLVM IR 조각으로 내려가는 데 필요한 정보만 모은다.
11. 그 다음에 지역변수, 산술, if, for, 포인터 순서로 넓힌다.

중요한 공부 방식:

- 한 번에 C 전체를 만들려고 하지 않는다.
- 매 단계마다 "지금 AST에는 어떤 정보가 있고, IR에는 어떤 정보가 필요한가?"를 비교한다.
- 파서 문제가 해결되지 않았는데 IR 생성으로 넘어가지 않는다.
- IR 생성에서 막히면 먼저 같은 코드를 C로 작성하고 `clang -S -emit-llvm` 결과를 관찰해 본다.

## 13. 참고용 작은 실험 아이디어

완성 테스트 파일을 만들기보다, 아래처럼 짧은 입력을 하나씩 직접 넣어 보며 확인한다.

Lexer 실험:

- `Int x;`
- `Int *p;`
- `Char s[4];`
- `return 123;`
- `"hello\n"`

Parser 실험:

- 전역변수 하나.
- 함수 선언 하나.
- 빈 함수 본문 하나.
- `return` 하나가 있는 함수 본문.

Semantic 실험:

- 선언된 변수 사용.
- 선언되지 않은 변수 사용.
- 포인터가 아닌 값에 `*`.
- 함수 인자 개수 불일치.
- 반복문 밖의 `break`.

IR 실험:

- 정수 반환.
- 정수 덧셈 반환.
- 지역변수 저장 후 반환.
- 단순 `if`.
- 단순 `for`.

## 14. 다음 학습 질문

다음에 다시 이어갈 때는 아래 질문 중 하나를 고르면 좋다.

- 지금 코드가 컴파일되게 만들기 위해 첫 번째로 봐야 할 오류는 무엇인가?
- `parseBlock()`은 어떤 문장 종류부터 처리해야 하는가?
- 표현식 우선순위 파서는 어떤 함수 경계로 나누면 좋은가?
- LLVM IR에서 변수의 값과 주소는 왜 다르게 다뤄야 하는가?
- `Int main() { return 1; }`를 AST로 그리면 어떤 노드들이 생기는가?

추천 첫 질문:

```text
현재 코드에서 Int main() { return 1; } 하나를 파싱하려면 Parser.cpp의 어느 빈칸부터 채워야 할까?
```
